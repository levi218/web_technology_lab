doctype html
html
	include head.pug
	body
		include menu.pug

		div(class="pagebody")
			h1 Item list
			a(href="/add") Add new item auction
			div
				each item in data
					a(href="/detail/"+item.itemId,class="item")
						img(src="/img/"+item.picture)
						span(class="itemName")= item.name
